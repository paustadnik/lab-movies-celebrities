<h2>Movie details</h2>
<h3>{{movie.title}}</h3>
<h4>{{movie.genre}}</h4>
<span>{{movie.plot}}</span>
<ul>{{#each movie.cast}}
    <li>{{this.name}}</li>
    {{else}}
    There are no celebrities to show
    {{/each}}
    
</ul>
<form action="/movies/{{movie.id}}/delete" method="post">
    <button type="submit">Delete</button>
</form>
<a href="/movies/{{movie.id}}/edit">Edit</a>